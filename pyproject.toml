[project]
name = "mornis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.116.1",
    "fastapi-cli>=0.0.10",
    "googlemaps>=4.10.0",
]
